{
  "name": "watering-diary-api",
  "version": "0.1.0",
  "main": "dist/src/{index.js,functions/*.js}",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start"
  },
  "dependencies": {
    "@azure/data-tables": "^13.3.0",
    "@azure/functions": "^4.5.1",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.2"
  }
}
